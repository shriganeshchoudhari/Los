name: OpenAPI to TypeScript (Decisions) Drift Check

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

jobs:
  generate-and-drift-check:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install frontend deps
        working-directory: ./frontend
        run: |
          npm ci

      - name: Generate OpenAPI TS types
        working-directory: ./frontend
        run: |
          npx openapi-typescript ../openapi/decisions-openapi.yaml --output openapi/generated/decisions.d.ts

      - name: Drift Check (compare with committed)
        working-directory: ./frontend
        run: |
          if ! git diff --exit-code openapi/generated/decisions.d.ts; then
            echo 'OpenAPI TS types drift detected. Please regenerate to align with the contract.';
            exit 1;
          fi

      - name: Archive generated types
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: decisions-openapi-types
          path: frontend/openapi/generated/decisions.d.ts
