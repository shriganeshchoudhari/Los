apiVersion: apps/v1
kind: Deployment
metadata:
  name: dsa-service
  labels:
    app: los
    service: dsa-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: los
      service: dsa-service
  template:
    metadata:
      labels:
        app: los
        service: dsa-service
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "3008"
        prometheus.io/path: "/metrics"
    spec:
      serviceAccountName: los-service
      securityContext:
        runAsNonRoot: true
        runAsUser: 1001
        fsGroup: 1001
      containers:
        - name: dsa-service
          image: ${ECR_REGISTRY}/dsa-service:${IMAGE_TAG}
          imagePullPolicy: Always
          ports:
            - containerPort: 3008
              name: http
          env:
            - name: NODE_ENV
              value: "production"
            - name: PORT
              value: "3008"
            - name: DB_HOST
              valueFrom:
                configMapKeyRef:
                  name: los-config
                  key: DB_HOST
            - name: DB_PORT
              value: "5432"
            - name: DB_USERNAME
              valueFrom:
                secretKeyRef:
                  name: los-secrets
                  key: DB_USERNAME
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: los-secrets
                  key: DB_PASSWORD
            - name: DB_NAME
              value: "los_dsa"
            - name: KAFKA_BROKERS
              valueFrom:
                configMapKeyRef:
                  name: los-config
                  key: KAFKA_BROKERS
            - name: DSA_JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: los-secrets
                  key: DSA_JWT_SECRET
            - name: ENCRYPTION_KEY
              valueFrom:
                secretKeyRef:
                  name: los-secrets
                  key: ENCRYPTION_KEY
            - name: ALLOWED_ORIGINS
              valueFrom:
                configMapKeyRef:
                  name: los-config
                  key: DSA_ALLOWED_ORIGINS
            - name: OTEL_SERVICE_NAME
              value: "dsa-service"
            - name: OTEL_EXPORTER_OTLP_ENDPOINT
              value: "http://otel-collector:4318"
            - name: OTEL_TRACES_SAMPLER
              value: "parentbased_always_on"
            - name: OTEL_PROPAGATORS
              value: "b3,tracecontext"
            - name: NODE_OPTIONS
              value: "-r @opentelemetry/auto-instrumentations-node/register"
          resources:
            requests:
              memory: "256Mi"
              cpu: "150m"
            limits:
              memory: "512Mi"
              cpu: "300m"
          livenessProbe:
            httpGet:
              path: /health
              port: 3008
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: 3008
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 3
          volumeMounts:
            - name: tmp
              mountPath: /tmp
      volumes:
        - name: tmp
          emptyDir: {}
      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: topology.kubernetes.io/zone
          whenUnsatisfiable: ScheduleAnyway
          labelSelector:
            matchLabels:
              app: los
              service: dsa-service

---
apiVersion: v1
kind: Service
metadata:
  name: dsa-service
  labels:
    app: los
    service: dsa-service
spec:
  type: ClusterIP
  ports:
    - port: 3008
      targetPort: 3008
      protocol: TCP
      name: http
  selector:
    app: los
    service: dsa-service

---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: dsa-service
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: dsa-service
  minReplicas: 2
  maxReplicas: 10
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: 80
  behavior:
    scaleDown:
      stabilizationWindowSeconds: 300
      policies:
        - type: Percent
          value: 10
          periodSeconds: 60
    scaleUp:
      stabilizationWindowSeconds: 0
      policies:
        - type: Percent
          value: 100
          periodSeconds: 15
        - type: Pods
          value: 4
          periodSeconds: 15
      selectPolicy: Max

---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: dsa-service
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: los
      service: dsa-service
